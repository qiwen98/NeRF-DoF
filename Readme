### for offline running
> npm run dev

### for deploy
> npm run build
> npm run deploy

### go to gh-pages branch
#### update the model file


#### all things mentioned above done with deploy.sh

#### in vite config.js the static models directory is placed under public
